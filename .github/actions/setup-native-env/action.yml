name: Setup Native Env
description: Install native GUI/build dependencies for CI jobs.

inputs:
  install-xvfb:
    description: Install xvfb for headless Linux test runs.
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Install Linux GUI build dependencies
      if: runner.os == 'Linux'
      shell: bash
      run: |
        packages=(
          libgtk-4-dev
          libepoxy-dev
          libx11-dev
          libgl1-mesa-dev
          libgirepository1.0-dev
          pkg-config
        )
        if [[ "${{ inputs.install-xvfb }}" == "true" ]]; then
          packages=(xvfb "${packages[@]}")
        fi
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends "${packages[@]}"

    - name: Install macOS GUI build dependencies
      if: runner.os == 'macOS'
      shell: bash
      run: |
        brew install gtk4 libepoxy gobject-introspection pkg-config

    - name: Set up Windows MinGW toolchain
      if: runner.os == 'Windows'
      id: msys2
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        release: false
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-gcc-libs
          mingw-w64-x86_64-gtk4
          mingw-w64-x86_64-libepoxy
          mingw-w64-x86_64-gobject-introspection
          mingw-w64-x86_64-pkg-config

    - name: Prepend Windows MinGW toolchain to PATH
      if: runner.os == 'Windows'
      shell: pwsh
      env:
        MSYS2_LOCATION: ${{ steps.msys2.outputs.msys2-location }}
      run: |
        "$env:MSYS2_LOCATION\mingw64\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
